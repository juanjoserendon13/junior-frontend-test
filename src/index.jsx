import React from 'react';
import { render } from 'react-dom';
import App from './components/App';

// File JSX that render the whole application in the main html

render(<App />, document.getElementById('root'));
